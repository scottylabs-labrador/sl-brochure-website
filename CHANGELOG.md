# Changelog

## 2026-08-29

- Desktop hero Scottie is the complete 3-piece silhouette (head/body, hindquarters/tail, and U-shaped belly/feet bar), exported as one SVG at native scale so pieces stay aligned (`object-fit: contain`).
- Desktop frame insets were tightened so snout, ears, and tail tip stay inside `.cover` instead of clipping under `overflow: hidden`. Phone cover layout is unchanged.
- Desktop Scottie is centered in a larger frame (about 9% right, 11% top) so snout and tail tip have visible padding from every cover edge.
- Header scroll-spy now highlights **Committees** when that section is in view. Evan (Slack #leadership-26-27) reported it never became active; the old IntersectionObserver `threshold` skipped the tall three-committee spread. Spy is now a document-order scan against a line below the sticky header, on both desktop and narrow layouts.
- Top nav lists all **7 committees** (Tech, Finance, Labrador, Design, Foundry, Events, Outreach) instead of one lumped Committees item, with scroll-spy on each — including nested Finance, Design, and Outreach. Requested by Thomas.
- Header order is now Outreach → Join → Calendar to match the page layout (Evan, Slack #leadership-26-27).
- Print stylesheet lays the live site out as a two-sided letter landscape trifold (outside: Events | Say Hello | Cover; inside: Tech | Labrador | Foundry), matching the Figma folds — not a screenshot of the scrolling page. Sticky nav and screen-only chrome are hidden when printing. **Evan** suggested this in Slack #leadership-26-27 (optional thought that a 1-to-1 site should print as a real brochure). **Thomas Kanz** gave the go-ahead in the same thread and asked for proof pages produced by the live app. Screen/mobile layout is unchanged. Preview at `/print`; File → Print from `/` or `/print`; download `/brochure.pdf`.
- Print panel density now matches the Figma O-week sheets. Thomas compared the live print to Figma and said the spacing didn’t match: too much unused cyan, a postage-stamp cover Scottie, and empty bands under Design/Foundry. Print CSS now tightens panel padding, grows photos/logos/cloud to the column bottoms, enlarges corner motifs, and sizes the cover Scottie so the head sits near the top of the right panel and the feet near the title (letter landscape trifold, live copy). Screen scrolling layout is unchanged.
- Print inside no longer flex-pushes Design and Foundry Interested to the column bottoms (`margin-top: auto`). Thomas called out the mid-column holes between the Labrador schedule and Design, and between Foundry committee copy and Interested. Those sections now stack from the top with Figma-like section spacing; leftover room stays at the bottom of the panel.
